html
  head
    title Bangazon - All Products
    include stylesheets.pug
  body
    include partials/nav.pug
    block content
      each product in prodArray
        ul
          a(href=`/product/${product.id}`) #{product.product_name}
          li #{product.description}
          li #{product.listing_date}
          li $#{product.price}