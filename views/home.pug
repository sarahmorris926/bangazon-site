extends index.pug

block content
  h1 All
  each product in productsByDate
    ul
      a(href=`/product/${product.id}`) #{product.product_name}
      li #{product.description}
      li #{product.listing_date}
      li $#{product.price}.00
