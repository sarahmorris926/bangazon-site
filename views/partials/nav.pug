nav(class="navbar navbar-expand-lg navbar-light bg-light")  
      if session.passport
        span #{session.passport.user.username}  
        -var id = session.passport.user.id
          div(class="collapse navbar-collapse" id="navbarNavAltMarkup")
            div(class="navbar-nav")
              a(class="nav-item nav-link" href="/") Home
              a(class="nav-item nav-link" href="/myproducts") My Products
              a(class="nav-item nav-link" href="/productcategories") Product Categories 
              a(class="nav-item nav-link" href="/allproducts") All Products
              a(class="nav-item nav-link" id="profile" href=`/profile/${id}`) Profile
              a(class="nav-item nav-link" href=`/cart/${id}`) Cart
                form(method="POST")
                  button(type="submit" formaction="/logout") logout
